{
  "name": "xword-normalize",
  "version": "0.1.0",
  "description": "Normalize messy ASCII crossword grids to a consistent format",
  "type": "module",
  "main": "dist/format.js",
  "bin": {
    "xword-normalize": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "tsc && node --test dist"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
